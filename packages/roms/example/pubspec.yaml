name: roms_example
description: ROMs example.

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  annotations:
    path: ../../annotations
  roms:
    path: ../../roms

dev_dependencies:
  lint: ^2.8.0
  test: ^1.30.0

