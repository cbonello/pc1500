name: pc1500
version: 0.1.0
description: Sharp PC-1500 pocket computer emulator.
homepage: https://github.com/cbonello/
repository: https://github.com/cbonello/pc1500
issue_tracker: https://github.com/cbonello/pc1500/issues
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  device:
    path: packages/device
  flutter_riverpod: ^2.6.1
  google_fonts: ^6.2.1
  json_annotation: ^4.9.0
  lcd:
    path: packages/lcd
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.14
  json_serializable: ^6.9.4
  lint: ^2.8.0

flutter:
  uses-material-design: true
  assets:
    - assets/systems/
